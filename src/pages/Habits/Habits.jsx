import { BaseLayout, AddHabit } from "../../components";
import { useRoot } from "../../store";

const Habits = () => {
  return (
    <BaseLayout>
      <AddHabit />
    </BaseLayout>
  );
};

export default Habits;
